# Plan — Renommage Gemini → Groq (dette de nommage)

## 1. Objectif

Supprimer toute référence à "Gemini" dans le code, les noms de fichiers, les commentaires et la documentation. Le projet utilise **Groq (llama-3.3-70b)** via `@ai-sdk/groq` — les noms doivent le refléter.

## 2. Périmètre

**CE QUI EST TOUCHÉ** : fichiers source backend, frontend, documentation, .env (commentaire)
**CE QUI NE SERA PAS TOUCHÉ** : le schéma Prisma, les routes API (`/api/v2/*`), les middlewares, la logique métier, les tests existants, le package.json (le package `@ai-sdk/groq` est déjà correct)

## 3. Fichiers à modifier — liste complète

### Fichiers à RENOMMER (3)
| Ancien nom | Nouveau nom |
|---|---|
| `backend/src/services/gemini.ts` | `backend/src/services/groq.ts` |
| `backend/src/infrastructure/services/GeminiIAService.ts` | `backend/src/infrastructure/services/GroqIAService.ts` |
| `backend/src/scripts/list-gemini-models.ts` | `backend/src/scripts/list-groq-models.ts` |

### Symboles à renommer (4)
| Ancien | Nouveau | Défini dans |
|---|---|---|
| `geminiModel` | `groqModel` | `services/groq.ts` |
| `generateWithGemini` | `generateWithGroq` | `services/groq.ts` |
| `GeminiIAService` | `GroqIAService` | `infrastructure/services/GroqIAService.ts` |
| `geminiIAService` (variable locale) | `groqIAService` | `infrastructure/config/container.ts` |

### Fichiers à modifier (imports + renommages) — 6 fichiers backend
| Fichier | Changements |
|---|---|
| `infrastructure/config/container.ts` | Import `GroqIAService` au lieu de `GeminiIAService`, variable `groqIAService` |
| `infrastructure/http/controllers/AIController.ts` | Import `generateWithGroq` depuis `services/groq` |
| `infrastructure/http/controllers/AssistantController.ts` | Import `groqModel` depuis `services/groq` |
| `infrastructure/http/controllers/OnboardingPEBSController.ts` | Import `generateWithGroq` depuis `services/groq` |
| `infrastructure/http/controllers/TimetableAutoController.ts` | Import `generateWithGroq` depuis `services/groq` |
| `domain/ports/services/IAService.ts` | JSDoc : "Groq via AI SDK" au lieu de "Google Gemini via AI SDK" |

### Commentaires/strings à corriger (2 fichiers backend)
| Fichier | Ligne | Avant → Après |
|---|---|---|
| `GeminiIAService.ts` (→ `GroqIAService.ts`) | 2 | "remplace Google Gemini" → "Adapter IA via Groq" |
| `services/gemini.ts` (→ `services/groq.ts`) | 38 | Le `console.error("Groq error:", ...)` est déjà correct |

### Frontend — 1 fichier
| Fichier | Changements |
|---|---|
| `components/LandingPage.tsx` | L49 : "Gemini AI" → "Groq AI" ; L165 : "Gemini AI" → "Groq AI" |

### Documentation — 5 fichiers
| Fichier | Changements |
|---|---|
| `ARCHITECTURE.md` | Remplacer les 4 références à gemini/GeminiIAService par groq/GroqIAService |
| `MODULE_INDEX.md` | Ligne 45 : `services/gemini.ts` → `services/groq.ts`, symboles `groqModel`/`generateWithGroq` |
| `AGENTS.md` | Ligne 50 : références aux symboles renommés |
| `README.md` | 8 occurrences : descriptions IA → Groq, modèle, env var, descriptions de flux |
| `.env` | Ligne 23 : commentaire "# AI CONFIGURATION (Google Gemini)" → "# AI CONFIGURATION (Groq)" |

### Script à supprimer ou réécrire
| Fichier | Action |
|---|---|
| `scripts/list-gemini-models.ts` → `scripts/list-groq-models.ts` | Renommer + remplacer les appels Google API par l'API Groq (`https://api.groq.com/openai/v1/models`), env var `GROQ_API_KEY` au lieu de `GOOGLE_GENERATIVE_AI_API_KEY` |

## 4. Étapes d'exécution

### Étape 1 — Renommer les fichiers (3 fichiers)
- `git mv backend/src/services/gemini.ts backend/src/services/groq.ts`
- `git mv backend/src/infrastructure/services/GeminiIAService.ts backend/src/infrastructure/services/GroqIAService.ts`
- `git mv backend/src/scripts/list-gemini-models.ts backend/src/scripts/list-groq-models.ts`

### Étape 2 — Renommer les symboles dans les fichiers renommés
- `services/groq.ts` : `geminiModel` → `groqModel`, `generateWithGemini` → `generateWithGroq`
- `GroqIAService.ts` : `class GeminiIAService` → `class GroqIAService`, commentaire L2
- `list-groq-models.ts` : réécrire pour utiliser l'API Groq (`GROQ_API_KEY`, endpoint Groq)

### Étape 3 — Mettre à jour les imports dans les 6 fichiers consommateurs
- `container.ts` : `GeminiIAService` → `GroqIAService`, `geminiIAService` → `groqIAService`
- `AIController.ts` : `generateWithGemini` → `generateWithGroq`, path `services/gemini` → `services/groq`
- `AssistantController.ts` : `geminiModel` → `groqModel`, path `services/gemini` → `services/groq`
- `OnboardingPEBSController.ts` : `generateWithGemini` → `generateWithGroq`, path `services/gemini` → `services/groq`
- `TimetableAutoController.ts` : `generateWithGemini` → `generateWithGroq`, path `services/gemini` → `services/groq`

### Étape 4 — Corriger le JSDoc dans le port domaine
- `domain/ports/services/IAService.ts` L3 : "Google Gemini" → "Groq"

### Étape 5 — Frontend
- `LandingPage.tsx` L49 : "Gemini AI" → "Groq AI"
- `LandingPage.tsx` L165 : "Gemini AI" → "Groq AI"

### Étape 6 — Documentation
- `ARCHITECTURE.md` : toutes les références gemini → groq
- `MODULE_INDEX.md` : chemins et symboles
- `AGENTS.md` : symboles et description
- `README.md` : 8 occurrences
- `.env` : commentaire L23

### Étape 7 — Vérification
- `cd backend && ./node_modules/.bin/tsc --noEmit` — doit compiler sans erreur
- Vérifier qu'aucune occurrence de "gemini"/"Gemini"/"GEMINI" ne subsiste (grep global)

## 5. Risques
- **Import cassés** si un fichier est oublié → le tsc le détectera immédiatement
- **Le script `list-groq-models.ts`** change d'API (Google → Groq) — à tester manuellement si besoin
- **La variable d'env `GOOGLE_GENERATIVE_AI_API_KEY`** n'est pas renommée dans `.env` (elle est définie par l'utilisateur) — on ne touche qu'au commentaire

## 6. Critères de validation
- `tsc --noEmit` backend clean
- `grep -ri "gemini" backend/src/` retourne 0 résultat
- `grep -ri "gemini" frontend/src/` retourne 0 résultat
- `grep -ri "gemini" *.md` retourne 0 résultat (hors git history)
- Les 3 fichiers renommés existent avec les bons noms
