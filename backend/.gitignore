node_modules
# Keep environment variables out of version control
.env
.env.*
!.env.example
!.env.test.example

# Cache incrémental tsc (accélère les tsc --noEmit répétés, notamment le hook pre-commit)
*.tsbuildinfo

/src/generated/prisma

# Déclarations statistiques générées (par école) — artefacts runtime, jamais versionnés
storage/statistical-submissions/
storage/minedub-reports/
storage/hr-documents/
